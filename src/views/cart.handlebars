<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Carrito de Compras</title>
</head>
<body>
    <h1>Carrito de Compras</h1>
    <ul>
        {{#each cartItems}}
            <li>{{this.name}} - Cantidad: {{this.quantity}} <button onclick="removeFromCart('{{this.id}}')">Eliminar</button></li>
        {{/each}}
    </ul>
    <button onclick="checkout()">Realizar Compra</button>
</body>
</html>
