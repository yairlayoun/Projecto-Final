<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Catálogo de Productos</title>
</head>
<body>
    <h1>Catálogo de Productos</h1>
    <ul>
        {{#each products}}
            <li>{{this.name}} - Precio: {{this.price}} <button onclick="addToCart('{{this.id}}')">Agregar al Carrito</button></li>
        {{/each}}
    </ul>
</body>
</html>
